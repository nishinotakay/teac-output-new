<div class="chat-message">
  <% if chat_message.user.id != current_user.id %>
    <div class="chat-message__partner">
      <% if @chat_room_user.profile.image.persisted? %>
        <%= image_tag chat_message.user.profile&.image, class: "chat-room__partner-img" %>
      <% else %>
        <div class="chat-room__partner-default-img"></div>
      <% end %>

      <div class="chat-message__content">
        <%= chat_message.content %>
      </div>
    </div>
  <% else %>
    <div class="chat-message__current-user">
      <div class="chat-message__content main-color-bg">
        <%= chat_message.content %>
      </div>
    </div>
  <% end %>
</div>
