<div class="text-center w-50 m-auto pb-2 border-bottom border-1 border-dark"><h2>完了</h2></div>

  <div class="d-flex justify-content-center mt-4 pb-5 text-center">
    <div class="charge-guide">1.入力</div>
    <div class="charge-guide">2.確認</div>
    <div class="charge-guide current-guide">3.完了</div>
  </div>

  <div class="container text-center mb-5">
    <p class="fs-4 mb-5">料金設定が完了しました。</p>
    <% if current_admin.charge_plan.present? %>
      <p><%= link_to "決済設定 画面に移動する", admins_charge_plan_path(current_admin.charge_plan.id), class: 'fs-5 text-dark' %></p>
    <% end %>
  </div>
