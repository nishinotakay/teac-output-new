<%= javascript_pack_tag "admins/charge" %>
<div class="text-center"><h2>決済設定画面</h2></div>
<p class="text-center mt-4 mb-5">決済設定が作成されていません。<br>下記フォームより決済設定を行ってください。</p>

<%= form_with model: @charge_plan, url: confirm_admins_charge_plans_path, method: :get, local: true do |f| %>
  <div class="field">
    <%= f.label :price %>
    <%= f.number_field :price, id: "price_field", step: "100", min: "0", class: 'text-center form-control-sm' %>
  </div>
  <div class="field mt-3">
    <%= f.label :quantity %>
    <%= f.number_field :quantity, id: "quantity_field", step: "10", min: "0", class: 'text-center form-control-sm' %>
  </div>
  <div class="field mt-3">
    <%= f.label :charge_type %>
    <%= f.select :charge_type, [["定額決済", "定額決済"], ["一括払い", "一括払い"], ["無料", "無料"]], { include_blank: "選択してください" }, { class: 'custom-select-field' } %>
  </div>
  <div class="field mt-3">
    <p id="total_amount">合計金額</p>
    <!-- <%= @charge_plan.amount %> -->
  </div>
  <div class="text-center mt-4 mb-4">
    <%= f.submit "確認画面へ", class: 'mt-3' %>
  </div>
<% end %>
