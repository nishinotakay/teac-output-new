<p><%= t('.greeting', recipient: @email) %></p>

<p><%= t('.instruction') %></p>

<p><%= link_to t('.action'), @resource.tenant_id.present? ? tenant_admin_admin_confirmation_url(tenant_id: @resource.tenant_id, confirmation_token: @token) : confirmation_url(@resource, confirmation_token: @token) %></p>
